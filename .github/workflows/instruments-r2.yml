name: Instruments - Cloudflare R2

on:
  push:
    branches: [main]
    paths:
      - 'public/samples/**'
      - 'server/configureInstrumentCors.js'
      - 'server/syncR2Assets.js'
      - '.github/workflows/instruments-r2.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-r2-instruments
  cancel-in-progress: false

jobs:
  sync:
    if: ${{ vars.R2_DEPLOY_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: server/package-lock.json
      - name: Configure instrument audio origins
        run: node server/configureInstrumentCors.js
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          INSTRUMENT_R2_BUCKET: ${{ vars.INSTRUMENT_R2_BUCKET }}
          INSTRUMENT_ALLOWED_ORIGINS: https://polymathmusician67.com,https://www.polymathmusician67.com,https://polymath-musician.pages.dev,https://scaling-preview.polymath-musician.pages.dev,http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174
      - run: npm ci --prefix server
      - name: Upload changed instrument recordings
        run: npm --prefix server run r2:sync-instruments
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          INSTRUMENT_R2_BUCKET: ${{ vars.INSTRUMENT_R2_BUCKET }}
          INSTRUMENT_R2_ACCESS_KEY_ID: ${{ secrets.INSTRUMENT_R2_ACCESS_KEY_ID }}
          INSTRUMENT_R2_SECRET_ACCESS_KEY: ${{ secrets.INSTRUMENT_R2_SECRET_ACCESS_KEY }}
          INSTRUMENT_ASSET_RELEASE: ${{ vars.VITE_ASSET_RELEASE }}
          INSTRUMENT_ALLOWED_ORIGINS: https://polymathmusician67.com,https://www.polymathmusician67.com,https://polymath-musician.pages.dev,https://scaling-preview.polymath-musician.pages.dev,http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174
          INSTRUMENT_CONFIGURE_CORS: 'false'
