name: Frontend - Cloudflare Pages

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'functions/**'
      - 'public/**'
      - 'index.html'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/frontend-cloudflare.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-pages-production
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
      VITE_ASSET_BASE_URL: ${{ vars.VITE_ASSET_BASE_URL }}
      VITE_ASSET_RELEASE: ${{ vars.VITE_ASSET_RELEASE }}
      VITE_BUILD_SHA: ${{ github.sha }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Reject high-severity production dependency vulnerabilities
        run: npm audit --omit=dev --audit-level=high
      - run: npm run lint
      - name: Run browser music behavior tests
        run: |
          npm run test:performance
          npm run test:teacher-senses
          npm run test:teacher-coaching
          npm run test:teacher-ar
          npm run test:create-music
          npm run test:falling-notes
          npm run test:teacher-hands
          npm run test:teacher-voice
          npm run test:teacher-animation
          npm run test:guitar-voicing
          npm run test:auth-phone
      - run: npm run build
      - run: node scripts/pruneRemoteAssets.mjs
      - name: Deploy production frontend
        if: ${{ github.ref == 'refs/heads/main' && vars.CLOUDFLARE_PAGES_ENABLED == 'true' }}
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=polymath-musician --branch=main
