muscriptor==0.3.0
# 1.7.11 through 1.10.0 can corrupt per-worker queue tracking on endpoints
# mounted to network volumes. Keep the fixed floor explicit and auditable.
runpod>=1.10.1,<2
soundfile>=0.14,<1
safetensors>=0.4,<1
